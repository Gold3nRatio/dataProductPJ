## have user enter the desire package name
pack <- available.packages()
pack['ggplot2', c('Depends', 'Imports')]
pack['ggplot2', 'Imports']

## show the dependencies

## find out of the current user already have those packages install

## check box
